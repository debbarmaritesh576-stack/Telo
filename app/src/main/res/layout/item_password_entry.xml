<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Initial Avatar -->
        <TextView
            android:id="@+id/tv_pass_initial"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:gravity="center"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:background="@drawable/bg_avatar"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Strength Dot -->
        <ImageView
            android:id="@+id/iv_pass_strength"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:src="@drawable/ic_file"
            app:layout_constraintTop_toTopOf="@id/tv_pass_initial"
            app:layout_constraintEnd_toEndOf="@id/tv_pass_initial" />

        <!-- Title -->
        <TextView
            android:id="@+id/tv_pass_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginStart="12dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/tv_pass_initial"
            app:layout_constraintEnd_toStartOf="@id/btn_pass_favorite" />

        <!-- Username -->
        <TextView
            android:id="@+id/tv_pass_username"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/on_surface_variant"
            android:layout_marginStart="12dp"
            app:layout_constraintTop_toBottomOf="@id/tv_pass_title"
            app:layout_constraintStart_toEndOf="@id/tv_pass_initial"
            app:layout_constraintEnd_toStartOf="@id/btn_pass_favorite" />

        <!-- URL -->
        <TextView
            android:id="@+id/tv_pass_url"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textColor="@color/on_surface_variant"
            android:layout_marginStart="12dp"
            app:layout_constraintTop_toBottomOf="@id/tv_pass_username"
            app:layout_constraintStart_toEndOf="@id/tv_pass_initial"
            app:layout_constraintEnd_toStartOf="@id/btn_pass_favorite" />

        <!-- Favorite -->
        <ImageButton
            android:id="@+id/btn_pass_favorite"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_favorite_border"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btn_pass_copy" />

        <!-- Copy -->
        <ImageButton
            android:id="@+id/btn_pass_copy"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_copy"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>